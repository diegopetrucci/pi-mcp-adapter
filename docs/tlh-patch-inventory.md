# TLH Patch Inventory

This table records the deliberate fork-only deltas and safeguards that must survive future released-tag intakes for `diegopetrucci/pi-mcp-adapter`. Read [`docs/UPSTREAM-SYNC.md`](./UPSTREAM-SYNC.md) first.

| Delta or safeguard | Why | Key files | Tests | Re-verify on intake? |
| --- | --- | --- | --- | --- |
| Dim connected-server footer in MCP status UI | TLH intentionally dims the MCP footer and appends only actually connected server names so the status line matches the rest of the footer styling and is easier to scan. This is a fork presentation delta, not an upstream behavior contract. | `init.ts` | `__tests__/init-status-bar.test.ts`, `__tests__/init-status.test.ts` | yes |
| Lazy startup facade / heavy runtime boundary | TLH keeps startup cheap by separating the model-facing facade and direct-tool/cache surface from the heavier runtime. Registration, proxy/direct execution, lifecycle ownership, and deferred startup must retain the boundary without changing runtime semantics. | `startup-mcp-facade.ts`, `direct-tool-surface.ts`, `mcp-runtime.ts`, `index.ts`, `direct-tools.ts` | `__tests__/startup-mcp-facade.test.ts`, `__tests__/mcp-runtime.test.ts`, `__tests__/index-lifecycle.test.ts`, `__tests__/index-import-closure.test.ts`, `__tests__/direct-tools.test.ts` | yes |
| Context-bounded model-facing surface | TLH preserves a stable, bounded proxy description and keeps context-heavy additions opt-in: namespace proxy tools default off, `scriptMode` defaults off, `freezeDirectTools` defaults on, the scripting skill is manual-only, and the worker receives no SDK, endpoint, credential, or host environment. The model-facing URL installer and Jev/TypeSafe semantic-search surface are deliberately not part of this fork. | `startup-mcp-facade.ts`, `direct-tool-surface.ts`, `mcp-code.ts`, `mcp-script-worker.mjs`, `types.ts`, `skills/mcp-scripting/SKILL.md`, `cli.js` | `__tests__/index-lifecycle.test.ts`, `__tests__/direct-tools.test.ts`, `__tests__/mcp-code.test.ts`, `__tests__/namespace-tools.test.ts`, `__tests__/collision-scan-lazy.test.ts`, `__tests__/startup-mcp-facade.test.ts` | yes |
| Config ownership and read-only import boundary | Shared MCP files remain explicit shared targets; Pi-owned adapter overrides hold adapter-only fields. Host and `.agents` imports can be detected or explicitly adopted but are not silently activated or rewritten. Symlink/alias identity and multi-file previews are checked before mutation. | `config.ts`, `commands.ts`, `mcp-setup-panel.ts`, `onboarding-state.ts` | `__tests__/config.test.ts`, `__tests__/commands-edit.test.ts`, `__tests__/commands-onboarding.test.ts`, `__tests__/commands-direct-tools-refusal.test.ts`, `__tests__/mcp-setup-preview-refusal.test.ts`, `__tests__/exclusive-config.test.ts` | yes |
| Scoped package identity and trusted OIDC publishing | This fork is distributed for TLH as `@diegopetrucci/pi-mcp-adapter`. Upstream syncs must not silently revert the scoped identity, TLH-owned version/tag, exact install pin, package file completeness, public build, or npm trusted-publishing workflow. | `package.json`, `package-lock.json`, `.github/workflows/release.yml`, `README.md`, `CHANGELOG.md`, `dist/` | `__tests__/package-manifest.test.ts`, `public-exports.test.mjs`, `npm pack --dry-run --json` inspection | yes |
| URL-bound auth merge hardening (historical backport, superseded by released upstream ancestry) | TLH retains regression coverage for the e22e0d4/9e36c51 URL/auth merge guard so higher-precedence config cannot repoint an HTTP server while laundering lower-precedence credentials and `oauth: false` remains explicit. The earlier backport is no longer counted as a unique v2.36 fork delta because the released upstream ancestry now contains it. | `config.ts`, `__tests__/config.test.ts` | `__tests__/config.test.ts` covers two-source, three-source, env-template, per-auth-field, and `oauth: false` regressions | yes |

## Historical v2.11 intake walk

- Dim connected-server footer: re-walked via `npx vitest run __tests__/init-status-bar.test.ts`; footer dimming, connected-name filtering, and empty-state clearing passed on the v2.11 merged tree.
- Lazy startup facade / heavy runtime boundary: re-walked via `npx vitest run __tests__/startup-mcp-facade.test.ts __tests__/mcp-runtime.test.ts __tests__/index-lifecycle.test.ts __tests__/direct-tools.test.ts`.
- Scoped package identity and trusted publishing: the v2.11 release checks covered the scoped package identity, exact install pin, public files, and OIDC workflow.
- URL-bound auth merge hardening: re-walked via `npx vitest run __tests__/config.test.ts`; the higher-precedence URL/auth merge guard and `oauth: false` preservation passed.

## v2.36.0 final intake walk (2026-09-22)

- **Released boundary:** the explicit fork merge is `f0fb751243ba9c0ce363312a51b50f7ed1dd2e63`; its upstream parent is released tag `v2.36.0` at `c00e66b5b959f3327ebefddd93fffe8d402694a3`. This walk is released-tag-only: no `upstream/main` commits or coherent unreleased feature clusters are included.
- **Off-DAG release note:** upstream tag `v2.18.0` resolves to `b16d02d3ff34a88f71eeba1750f319f28bd6b34f`, which is not an ancestor of the fork merge. It is recorded as an off-DAG reference rather than claimed as an adopted ancestor; the reachable released history after that boundary is audited through the v2.36.0 tag.
- **Dim footer:** retained in `init.ts`; `npx vitest run __tests__/init-status-bar.test.ts __tests__/init-status.test.ts` passed (12 tests) for dimming, connected-name filtering, and upstream-compatible status behavior.
- **Lazy facade/runtime:** retained in `index.ts`, `startup-mcp-facade.ts`, `direct-tool-surface.ts`, and `mcp-runtime.ts`; the focused facade/runtime/lifecycle/direct-tool test set passed, including the no-heavy-import closure and runtime delegation checks.
- **Bounded model surface:** retained the explicit-off namespace and scripting defaults, stable proxy description, manual-only skill, and worker isolation. The released upstream URL installer, Jev/TypeSafe integration, source files, examples, package dependency, and generated public dist artifacts are excluded.
- **Config ownership:** retained shared-config versus Pi-owned override separation, detection-only host imports, explicit preview-before-mutation, symlink identity checks, and refusal tests for read-only/aliased destinations; the focused config/setup test set passed (118 tests across the selected files).
- **Package/release path:** `@diegopetrucci/pi-mcp-adapter@2.36.0`, README pin `pi install npm:@diegopetrucci/pi-mcp-adapter@2.36.0`, planned tag `tlh-v2.36.0`, audit-refreshed scoped lockfile via `npm audit fix --package-lock-only --ignore-scripts` (manifest and explicit pins unchanged), clean public `dist/`, and workflow dependency install/build before `npm publish --access public --provenance` passed `npx vitest run __tests__/package-manifest.test.ts`, `npm run build:public`, `npm run test:public-exports`, and `npm audit --audit-level=high`. The out-of-repository `npm pack --dry-run --json` check reported 132 entries / 3,227,241 unpacked bytes and included the required runtime roots, `dist`, skill, worker/helper, and bundle assets with no Jev/install artifacts.
- **Auth backport status:** the earlier TLH e22e0d4/9e36c51 backport is superseded by the released upstream ancestry, while its URL-binding regressions remain in the inventory and config test walk.

Known released-boundary residuals are intentional: the fork tag and trusted publish dispatch are release-operation work and are not created by this ticket; upstream changes after `c00e66b5b959f3327ebefddd93fffe8d402694a3` remain for a later released-tag intake.
