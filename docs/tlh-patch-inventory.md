# TLH Patch Inventory

This table records the deliberate fork-only deltas that must survive future upstream intakes for `diegopetrucci/pi-mcp-adapter`. Read [`docs/UPSTREAM-SYNC.md`](./UPSTREAM-SYNC.md) first.

| Delta | Why | Key files | Tests | Re-verify on intake? |
| --- | --- | --- | --- | --- |
| Dim connected-server footer in MCP status UI | TLH intentionally dims the MCP footer and appends the names of actually connected servers so the status line matches the rest of the footer styling and is easier to scan. This is a fork presentation delta, not an upstream behavior contract. | `init.ts` | `__tests__/init-status-bar.test.ts` asserts the footer is dimmed, includes only connected server names, and clears when no servers are configured. | yes |
| Lazy startup facade / heavy runtime boundary | TLH split startup into a lightweight facade and a heavier runtime module so command registration, proxy tool registration, and direct-tool cache behavior stay cheap at startup without changing runtime semantics. Upstream intakes touching bootstrap or tool registration need an explicit boundary check. | `startup-mcp-facade.ts`, `mcp-runtime.ts`, `index.ts`, `direct-tools.ts` | `__tests__/startup-mcp-facade.test.ts`, `__tests__/mcp-runtime.test.ts`, `__tests__/index-lifecycle.test.ts`, and `__tests__/direct-tools.test.ts` cover the no-heavy-import facade path, deferred on-demand startup, and v2.11-era execution delegation into the runtime/direct-tool executor. | yes |
| Scoped package identity and trusted-publishing release path | This fork is distributed for TLH as `@diegopetrucci/pi-mcp-adapter`, with npm trusted publishing documented and wired for the fork. Upstream syncs must not silently revert package identity, publish metadata, package file completeness, README exact install pin, or changelog/release-workflow evidence. | `package.json`, `.github/workflows/release.yml`, `README.md`, `CHANGELOG.md` | `__tests__/package-manifest.test.ts` verifies the scoped package name/version, published file list, README exact install pin aligned with the current `package.json` version, changelog coexistence, and trusted-publishing workflow evidence. | yes |
| URL-bound auth merge hardening (post-v2.11 backport) | TLH intentionally carries the upstream e22e0d4/9e36c51 hardening ahead of a later upstream intake so higher-precedence config sources cannot repoint an HTTP server URL while inheriting lower-precedence credentials. Keep `oauth: false` preserved across URL repointing. | `config.ts`, `__tests__/config.test.ts` | `__tests__/config.test.ts` covers two-source, three-source, env-template, per-auth-field, and `oauth: false` regressions. | yes |

## v2.11 final intake walk

- Dim connected-server footer in MCP status UI: re-walked via `npx vitest run __tests__/init-status-bar.test.ts`; footer dimming, connected-name filtering, and empty-state clearing still pass on the merged tree.
- Lazy startup facade / heavy runtime boundary: re-walked via `npx vitest run __tests__/startup-mcp-facade.test.ts __tests__/mcp-runtime.test.ts __tests__/index-lifecycle.test.ts __tests__/direct-tools.test.ts`; the lightweight facade, deferred runtime loading, and direct-tool/runtime delegation all still pass.
- Scoped package identity and trusted-publishing release path: re-walked via `npx vitest run __tests__/package-manifest.test.ts` plus an out-of-repo `npm pack --json --pack-destination "$TMPDIR"` inspection; the tarball still reports `@diegopetrucci/pi-mcp-adapter@2.10.2` and includes every manifest-declared runtime file.
- URL-bound auth merge hardening (post-v2.11 backport): re-walked via `npx vitest run __tests__/config.test.ts`; the higher-precedence URL/auth merge guard and `oauth: false` preservation still pass.
